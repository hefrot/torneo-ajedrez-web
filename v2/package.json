{
  "name": "hmena-chess-league-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "start": "node src/server.js",
    "worker": "node src/worker.js",
    "check": "node --check src/server.js && node --check src/worker.js && node --check scripts/migrate-history.mjs",
    "migrate:history": "node scripts/migrate-history.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0",
    "googleapis": "^159.0.0"
  }
}
