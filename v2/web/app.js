const $=s=>document.querySelector(s);const escapeHtml=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function jsonFetch(url,options){const response=await fetch(url,options);const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);return data;}async function loadStandings(){const body=$('#standings');body.innerHTML='<tr><td colspan="7">Cargando…</td></tr>';try{const rows=await jsonFetch('/api/standings');body.innerHTML=rows.length?rows.map(row=>`<tr><td>${row.rank}</td><td>${escapeHtml(row.name)}</td><td>${row.played}</td><td>${row.wins}</td><td>${row.draws}</td><td>${row.losses}</td><td><strong>${row.points}</strong></td></tr>`).join(''):'<tr><td colspan="7">La temporada aún no tiene jugadores activos o partidas validadas.</td></tr>';}catch(error){body.innerHTML=`<tr><td colspan="7">${escapeHtml(error.message)}</td></tr>`;}}$('#register-form').addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.currentTarget);const message=$('#register-message');message.textContent='Registrando…';try{const result=await jsonFetch('/api/registration',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form.entries()))});message.textContent=`Listo. Registro ${result.id} recibido; queda pendiente de verificación.`;event.currentTarget.reset();}catch(error){message.textContent=error.message;}});$('#refresh').addEventListener('click',loadStandings);loadStandings();
