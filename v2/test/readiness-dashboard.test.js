import test from'node:test';import assert from'node:assert/strict';import{openDatabase}from'../src/db.js';import{buildReadinessDashboard}from'../src/readiness-dashboard.js';
test('readiness reports incompatible pairs, ownership and missing decisions',()=>{const db=openDatabase(':memory:'),hash='0'.repeat(64);for(const [id,platform]of[['P1','lichess'],['P2','chesscom']]){db.prepare("INSERT INTO players(id,name,platform,username,registration_status) VALUES (?,?,?,?, 'registered')").run(id,id,platform,id);db.prepare("INSERT INTO player_accounts(id,player_id,platform,username,username_normalized,account_status,source_system,verification_source,verified_at,source_sha256) VALUES (?,?,?,?,?,'verified','test','test','2026-08-11T00:00:00Z',?)").run('A'+id,id,platform,id,id.toLowerCase(),hash);}const dashboard=buildReadinessDashboard(db);assert.equal(dashboard.status,'BLOCKED');assert.equal(dashboard.profileVerifiedPlayers,2);assert.equal(dashboard.ownershipVerifiedPlayers,0);assert.equal(dashboard.incompatiblePairCount,1);assert.ok(dashboard.missingRuleDecisions.length>0);assert.equal(dashboard.expectedSeries,1);assert.equal(dashboard.totalOfficialGames,3);db.close();});
